const USER_TYPE = {
    client: "client",
    coach: "vendor",
    admin: "admin",
};

const COACH_APPROVAL_STATUS = {
    pending: "pending",
    processed: "processed"
}

module.exports = { USER_TYPE, COACH_APPROVAL_STATUS, };